# Working chapter

`EU_ECU_FTA_Chapter_Revised.docx` is the revised working version supplied on July 7, 2026. It is included in the local package to align code, figures, tables, and text.

**Before making the repository public, confirm that the editor and publisher authorize distribution of this Word file.** If authorization has not been granted, remove the document and retain only this README until publication.

The DOCX is deliberately matched by `.gitignore`, so it remains in the local package but is not staged for a public GitHub repository until permission is confirmed.
